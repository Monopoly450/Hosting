/**
 * Единая шкала размеров иконок.
 *
 * Размеров в интерфейсе накопилось девятнадцать штук — от 10 до 48, причём
 * для ОДНОЙ и той же роли использовались разные: пустые состояния панелей
 * рисовались размерами 32, 36, 38, 40, 44 и 48, микроподписи — 10, 11, 12 и
 * 13. Отсюда ощущение «в одних вкладках иконки маленькие, в других большие».
 *
 * Ступеней намеренно мало: чем их больше, тем быстрее шкала снова расползётся.
 * Если нужного размера здесь нет — почти всегда верный ответ ближайшая
 * ступень, а не новое значение.
 */
export const ICON = {
  /** Микроподписи: время, размер файла, подсказка мелким шрифтом. */
  xs: 12,
  /** Кнопки, элементы списков, иконки внутри строк таблицы. */
  sm: 14,
  /** Заголовки секций, поля форм. */
  md: 16,
  /** Пункты бокового меню, заголовки карточек. */
  lg: 18,
  /** Крупные акценты: иконка раздела, значок в плитке. */
  xl: 20,
  /** Заголовок сущности (имя бакета, имя кластера БД). */
  xxl: 24,
  /** Заглушка внутри карточки или пустой таблицы. */
  placeholder: 32,
  /** Пустое состояние всей вкладки: «Доменов пока нет». */
  empty: 44,
};

export default ICON;
